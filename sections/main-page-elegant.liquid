{%- liquid
  assign tf = section.settings.title_font_family | default: 'default'
  assign tsd = section.settings.title_font_size_desktop | default: 0
  assign tsm = section.settings.title_font_size_mobile | default: 0
  assign bf = section.settings.body_font_family | default: 'default'
  assign bsd = section.settings.body_font_size_desktop | default: 0
  assign bsm = section.settings.body_font_size_mobile | default: 0
-%}
{%- if tf != 'default' or tsd > 0 or tsm > 0 or bf != 'default' or bsd > 0 or bsm > 0 -%}
  {% style %}
    {%- if tf != 'default' -%}
      #shopify-section-{{ section.id }} .elegant-page-hero__heading { font-family: var(--font-{{ tf }}--family); }
    {%- endif -%}
    {%- if tsd > 0 -%}
      #shopify-section-{{ section.id }} .elegant-page-hero__heading { font-size: {{ tsd }}px; }
    {%- endif -%}
    {%- if bf != 'default' -%}
      #shopify-section-{{ section.id }} .elegant-page-content,
      #shopify-section-{{ section.id }} .elegant-page-content > * { font-family: var(--font-{{ bf }}--family); }
    {%- endif -%}
    {%- if bsd > 0 -%}
      #shopify-section-{{ section.id }} .elegant-page-content { font-size: {{ bsd }}px; }
    {%- endif -%}
    @media screen and (max-width: 749px) {
      {%- if tsm > 0 -%}
        #shopify-section-{{ section.id }} .elegant-page-hero__heading { font-size: {{ tsm }}px; }
      {%- endif -%}
      {%- if bsm > 0 -%}
        #shopify-section-{{ section.id }} .elegant-page-content { font-size: {{ bsm }}px; }
      {%- endif -%}
    }
  {% endstyle %}
{%- endif -%}

<div
  class="elegant-page color-{{ section.settings.color_scheme }}"
  style="padding-bottom: {{ section.settings.padding_bottom }}px;"
>
  {% if section.settings.hero_image != blank %}
    <div
      class="elegant-page-hero-image-wrapper"
      style="position: relative; width: 100%; height: {{ section.settings.hero_height }}; overflow: hidden; margin-bottom: {{ section.settings.padding_top }}px;"
    >
      <div
        class="elegant-page-hero-image parallax-bg"
        style="position: absolute; top: -15%; left: 0; width: 100%; height: 130%; background-image: url('{{ section.settings.hero_image | image_url: width: 2000 }}'); background-size: cover; background-position: center;"
      ></div>
      <div
        class="elegant-page-hero-overlay"
        style="position: absolute; inset: 0; background-color: #000; opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"
      ></div>

      <div
        class="page-width-content"
        style="position: relative; height: 100%; padding-inline: var(--page-margin); display: flex; align-items: {% if section.settings.text_alignment == 'center' %}center{% else %}flex-end{% endif %}; justify-content: {% if section.settings.text_alignment == 'center' %}center{% else %}flex-start{% endif %}; padding-bottom: {% if section.settings.text_alignment == 'center' %}0{% else %}4rem{% endif %}; z-index: 2;"
      >
        <h1
          class="elegant-page-hero__heading has-image h1"
          style="margin: 0; color: #fff; text-align: {{ section.settings.text_alignment }};"
        >
          {{ page.title | escape }}
        </h1>
      </div>
    </div>
  {% else %}
    <div class="section page-width-content">
      <div
        class="elegant-page-hero text-only"
        style="text-align: {{ section.settings.text_alignment }}; padding-top: {{ section.settings.padding_top }}px; margin-bottom: 2.5rem;"
      >
        <h1
          class="elegant-page-hero__heading h1"
          style="margin: 0; text-align: {{ section.settings.text_alignment }};"
        >
          {{ page.title | escape }}
        </h1>
      </div>
    </div>
  {% endif %}

  <div class="section page-width-content">
    <div class="rte elegant-page-content">
      <rte-formatter>{{ page.content }}</rte-formatter>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', (event) => {
    if (typeof gsap !== 'undefined' && typeof ScrollTrigger !== 'undefined') {
      gsap.registerPlugin(ScrollTrigger);

      // Hero Text Animation
      gsap.fromTo(
        '.elegant-page-hero__heading',
        { y: 30, opacity: 0 },
        { y: 0, opacity: 1, duration: 1.2, ease: 'power3.out' }
      );

      // Editor Content Animation
      gsap.fromTo(
        '.elegant-page-content',
        { y: 30, opacity: 0 },
        { y: 0, opacity: 1, duration: 1.2, ease: 'power3.out', delay: 0.2 }
      );

      // Parallax Image Animation (If image exists)
      const parallaxBg = document.querySelector('.parallax-bg');
      if (parallaxBg) {
        gsap.to(parallaxBg, {
          yPercent: 15,
          ease: 'none',
          scrollTrigger: {
            trigger: '.elegant-page-hero-image-wrapper',
            start: 'top top',
            end: 'bottom top',
            scrub: true,
          },
        });
      }
    }
  });
</script>

{% schema %}
{
  "name": "Elegant Page",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Hero Settings"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image (Optional)",
      "info": "If an image is provided, it will create a full-width parallax banner. If empty, it will display a minimal text hero."
    },
    {
      "type": "select",
      "id": "hero_height",
      "label": "Hero Height (If Image is Used)",
      "options": [
        { "value": "50vh", "label": "Small" },
        { "value": "70vh", "label": "Medium" },
        { "value": "100vh", "label": "Full Screen" }
      ],
      "default": "70vh"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Image Overlay Opacity",
      "default": 30
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Heading Alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "Space Below Hero / Padding Top",
      "default": 100
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 80
    },
    {
      "type": "header",
      "content": "Title Typography"
    },
    {
      "type": "select",
      "id": "title_font_family",
      "label": "Title font family",
      "options": [
        { "value": "default", "label": "Default (preset)" },
        { "value": "heading", "label": "Heading" },
        { "value": "body", "label": "Body" },
        { "value": "subheading", "label": "Subheading" },
        { "value": "accent", "label": "Accent" }
      ],
      "default": "default"
    },
    {
      "type": "range",
      "id": "title_font_size_desktop",
      "min": 0,
      "max": 96,
      "step": 1,
      "unit": "px",
      "label": "Title size (desktop)",
      "info": "0 = use preset size.",
      "default": 0
    },
    {
      "type": "range",
      "id": "title_font_size_mobile",
      "min": 0,
      "max": 72,
      "step": 1,
      "unit": "px",
      "label": "Title size (mobile)",
      "default": 0
    },
    {
      "type": "header",
      "content": "Body Typography"
    },
    {
      "type": "select",
      "id": "body_font_family",
      "label": "Body font family",
      "options": [
        { "value": "default", "label": "Default (preset)" },
        { "value": "heading", "label": "Heading" },
        { "value": "body", "label": "Body" },
        { "value": "subheading", "label": "Subheading" },
        { "value": "accent", "label": "Accent" }
      ],
      "default": "default"
    },
    {
      "type": "range",
      "id": "body_font_size_desktop",
      "min": 0,
      "max": 48,
      "step": 1,
      "unit": "px",
      "label": "Body size (desktop)",
      "info": "0 = use preset size.",
      "default": 0
    },
    {
      "type": "range",
      "id": "body_font_size_mobile",
      "min": 0,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Body size (mobile)",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Elegant Page"
    }
  ]
}
{% endschema %}
