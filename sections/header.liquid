{% comment %}
  Header Redesign
  Based on references/header.html with Tailwind CSS converted to custom CSS.
{% endcomment %}

{% schema %}
{
  "name": "t:names.header",
  "class": "header-section",
  "settings": [
    {
      "type": "header",
      "content": "Advanced cart drawer"
    },
    {
      "type": "checkbox",
      "id": "cart_offer_enabled",
      "label": "Show offer bar",
      "default": true
    },
    {
      "type": "text",
      "id": "cart_offer_text",
      "label": "Offer bar text",
      "default": "Limited-time offer: complete your order today."
    },
    {
      "type": "header",
      "content": "Cart rewards"
    },
    {
      "type": "number",
      "id": "cart_reward_1_amount",
      "label": "Reward 1 minimum spend",
      "default": 200000
    },
    {
      "type": "text",
      "id": "cart_reward_1_text",
      "label": "Reward 1",
      "default": "Rp15.000 discount"
    },
    {
      "type": "number",
      "id": "cart_reward_2_amount",
      "label": "Reward 2 minimum spend",
      "default": 800000
    },
    {
      "type": "text",
      "id": "cart_reward_2_text",
      "label": "Reward 2",
      "default": "Rp30.000 discount"
    },
    {
      "type": "number",
      "id": "cart_reward_3_amount",
      "label": "Reward 3 minimum spend",
      "default": 2000000
    },
    {
      "type": "text",
      "id": "cart_reward_3_text",
      "label": "Reward 3",
      "default": "FREE international & national shipping"
    },
    {
      "type": "header",
      "content": "Available discount codes"
    },
    {
      "type": "checkbox",
      "id": "cart_show_available_codes",
      "label": "Show available discount codes",
      "default": true
    },
    {
      "type": "text",
      "id": "cart_discount_heading",
      "label": "Discount codes heading",
      "default": "Available offers"
    },
    {
      "type": "text",
      "id": "cart_discount_code_1",
      "label": "Discount code 1",
      "default": "WELCOME10"
    },
    {
      "type": "text",
      "id": "cart_discount_code_1_text",
      "label": "Discount code 1 description",
      "default": "10% off your first order"
    },
    {
      "type": "text",
      "id": "cart_discount_code_2",
      "label": "Discount code 2"
    },
    {
      "type": "text",
      "id": "cart_discount_code_2_text",
      "label": "Discount code 2 description"
    },
    {
      "type": "text",
      "id": "cart_discount_code_3",
      "label": "Discount code 3"
    },
    {
      "type": "text",
      "id": "cart_discount_code_3_text",
      "label": "Discount code 3 description"
    },
    {
      "type": "header",
      "content": "Cart product layout"
    },
    {
      "type": "range",
      "id": "cart_quantity_size_desktop",
      "label": "Quantity control size - desktop",
      "min": 24,
      "max": 44,
      "step": 1,
      "unit": "px",
      "default": 30
    },
    {
      "type": "range",
      "id": "cart_quantity_size_mobile",
      "label": "Quantity control size - mobile",
      "min": 24,
      "max": 40,
      "step": 1,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "cart_product_image_width_desktop",
      "label": "Product image width - desktop",
      "min": 60,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 82
    },
    {
      "type": "range",
      "id": "cart_product_image_width_mobile",
      "label": "Product image width - mobile",
      "min": 60,
      "max": 100,
      "step": 2,
      "unit": "px",
      "default": 74
    },
    {
      "type": "header",
      "content": "Cart trust messages"
    },
    {
      "type": "text",
      "id": "cart_trust_1_title",
      "label": "Trust item 1 title",
      "default": "Secure payment"
    },
    {
      "type": "text",
      "id": "cart_trust_1_text",
      "label": "Trust item 1 description",
      "default": "Safe & protected checkout"
    },
    {
      "type": "text",
      "id": "cart_trust_2_title",
      "label": "Trust item 2 title",
      "default": "Fast delivery"
    },
    {
      "type": "text",
      "id": "cart_trust_2_text",
      "label": "Trust item 2 description",
      "default": "Quick nationwide shipping"
    },
    {
      "type": "text",
      "id": "cart_trust_3_title",
      "label": "Trust item 3 title",
      "default": "Easy exchange"
    },
    {
      "type": "text",
      "id": "cart_trust_3_text",
      "label": "Trust item 3 description",
      "default": "Simple exchange process"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },

    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        { "value": "none", "label": "t:sections.header.settings.sticky_header_type.options__1.label" },
        { "value": "on-scroll-up", "label": "t:sections.header.settings.sticky_header_type.options__2.label" },
        { "value": "always", "label": "t:sections.header.settings.sticky_header_type.options__3.label" }
      ],
      "default": "always",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "header",
      "content": "Sticky Header Overrides"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_header_product",
      "default": false,
      "label": "Enable sticky header on product page"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_header_collection",
      "default": false,
      "label": "Enable sticky header on collection page"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_header_cart",
      "default": false,
      "label": "Enable sticky header on cart page"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_header_search",
      "default": false,
      "label": "Enable sticky header on search page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header",
      "default": true,
      "label": "t:sections.header.settings.enable_transparent_header.label"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_product",
      "default": false,
      "label": "Enable transparent header on product page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_collection",
      "default": true,
      "label": "Enable transparent header on collection page"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_page",
      "default": false,
      "label": "Enable transparent header on pages"
    },
    {
      "type": "checkbox",
      "id": "enable_transparent_header_search",
      "default": false,
      "label": "Enable transparent header on search page"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Scrolled State Override"
    },
    {
      "type": "color",
      "id": "scrolled_bg_color",
      "label": "Scrolled Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "scrolled_text_color",
      "label": "Scrolled Text Color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Currency Switcher"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_currency_ui",
      "label": "Use custom currency switcher in header",
      "info": "Replaces Nova app floating widget with a header-integrated dropdown. Uncheck to revert to Nova's default widget.",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "_header-logo"
    },
    {
      "type": "_header-menu"
    }
  ]
}
{% endschema %}

{%- liquid
  assign sticky_header_type = section.settings.sticky_header_type

  # Disable sticky header on specific pages unless explicitly enabled
  if template.name == 'product' and section.settings.enable_sticky_header_product == false
    assign sticky_header_type = 'none'
  elsif template.name == 'collection' and section.settings.enable_sticky_header_collection == false
    assign sticky_header_type = 'none'
  elsif template.name == 'list-collections' and section.settings.enable_sticky_header_collection == false
    assign sticky_header_type = 'none'
  elsif template.name == 'cart' and section.settings.enable_sticky_header_cart == false
    assign sticky_header_type = 'none'
  elsif template.name == 'search' and section.settings.enable_sticky_header_search == false
    assign sticky_header_type = 'none'
  endif

  assign transparent_header = false
  if template.name == 'index' and section.settings.enable_transparent_header
    assign transparent_header = true
  elsif template.name == 'product' and section.settings.enable_transparent_header_product
    assign transparent_header = true
  elsif template.name == 'collection' and section.settings.enable_transparent_header_collection
    assign transparent_header = true
  elsif template.name == 'list-collections' and section.settings.enable_transparent_header_collection
    assign transparent_header = true
  elsif template.name == 'page' and section.settings.enable_transparent_header_page
    assign transparent_header = true
  elsif template.name == 'search' and section.settings.enable_transparent_header_search
    assign transparent_header = true
  endif

  assign logo = settings.logo
  assign logo_height = settings.logo_height | default: 30
  assign logo_width = logo_height | times: logo.aspect_ratio | round
  assign logo_height_mobile = settings.logo_height_mobile | default: 20
  assign logo_width_mobile = logo_height_mobile | times: logo.aspect_ratio | round
-%}

{% style %}
  /* Custom Variables mapped from Reference Tailwind Config */
  .header-custom {
    --color-burgundy-100: #5d0f17;
    --color-off-white: #f2f0ed;
    --color-white: #ffffff;
    --color-dark-burgundy: #300000;
    --header-bg-color: var(--color-white);
    --header-text-color: #000000;

    --header-scrolled-bg: {{ section.settings.scrolled_bg_color }};
    --header-scrolled-text: {{ section.settings.scrolled_text_color }};

    --header-height: 70px;
    --header-padding: 15px; /* gap-xxs */
    --nav-btn-height: 35px;
    --nav-btn-min-width: 70px;
    --nav-btn-hover-width: 120px;

    --icon-stroke-width: 1.5px; /* Ensure icons are visible */
  }

  @media screen and (min-width: 768px) {
    .header-custom {
      --header-height: 80px; /* h-nav */
      --header-padding: 30px; /* px-md approx */
      --nav-btn-height: 40px;
    }
  }

  /* Sticky & Transparent Logic */

  /* Stick announcement bar + header together as one unit on every page.
     Both sections live inside #header-group (see layout/theme.liquid), so
     making the wrapper sticky keeps them locked together in normal flow:
     no overlap, no content jump. Keep the Header section's own
     "Sticky header type" = None so it doesn't switch to position:fixed and
     escape this wrapper. */
  #header-group {
    position: -webkit-sticky;
    position: sticky;
    top: 0;
    z-index: 100;
  }

  .header-custom.header-wrapper {
    position: relative;
    z-index: 100;
    width: 100%;
    transition: transform 0.3s ease, background-color 0.3s ease, color 0.3s ease, top 0.3s ease;
    background-color: var(--header-bg-color);
    color: var(--header-text-color);
    /* No will-change here: it creates a containing block that traps the
       position:fixed menu drawer inside the header, offsetting it below
       the announcement bar and pushing its bottom past the viewport. */
  }

  /* While the menu drawer is open, hide the floating WhatsApp button so it
     doesn't overlap the drawer's bottom links (e.g. the currency switcher). */
  body:has(.header-custom details.menu-open) #wa-chat-btn-root {
    display: none;
  }

  .header-custom.header-wrapper[data-sticky-type='on-scroll-up'] {
    position: fixed;
    top: 0;
    width: 100%;
    /* Let JS handle the translateY via inline style */
  }

  .header-custom.header-wrapper[data-sticky-type='always'] {
    position: sticky;
    top: 0;
  }

  .header-custom.header-wrapper.is-transparent {
    position: absolute;
    /* Fallback to 0, dynamically populated by JS to calculate precise offset of siblings like announcement bar */
    top: var(--announcement-bar-height, 0px);
    left: 0;
    right: 0;
    background: transparent;
    color: var(--color-white);
    border-bottom: none;
    transform: none !important; /* Force prevent any negative translations from JS */
  }

  .header-custom.header-wrapper.is-scrolled {
    background-color: var(--header-scrolled-bg) !important;
    color: var(--header-scrolled-text) !important;
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
    /* Reduce height when scrolled */
    --header-height: 60px;
  }

  @media screen and (min-width: 768px) {
    .header-custom.header-wrapper.is-scrolled {
       --header-height: 65px;
    }
  }

  /* When transparent and on-scroll-up/always, we need absolute/fixed depending on scrolled state */
  /* OVERRIDES for sticky behavior */
  .header-custom.header-wrapper.is-scrolled[data-sticky-type='on-scroll-up'],
  .header-custom.header-wrapper.is-scrolled[data-sticky-type='always']  {
     position: fixed;
     top: 0 !important; /* Reset any top offsets when it becomes a sticky block */
  }

  .header-custom.header-wrapper.is-hidden {
     transform: translateY(-100%);
  }

  .header-inner {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    padding: 0 var(--header-padding);
    height: var(--header-height);
    width: 100%;
    margin: 0 auto;
    max-width: var(--page-width, 1440px);
    transition: height 0.3s ease; /* Ensure smooth height transition */
  }

  /* Left: Menu Button */
  .header-left {
    display: flex;
    align-items: center;
    gap: 8px;
    z-index: 3;
  }

  /* Hide default details summary from header-drawer */
  .header-left header-drawer > details > summary {
    display: none !important;
  }

  .btn-header-pill {
    display: flex;
    align-items: center;
    justify-content: center; /* Center icon when text is hidden */
    height: var(--nav-btn-height);
    min-width: 44px; /* Sufficient for a circle/pill */
    padding: 0 4px; /* Reduced padding, text margin handles spacing */
    border-radius: 9999px;
    background-color: transparent;
    color: inherit;
    border: none;
    cursor: pointer;
    overflow: hidden;
    /* Remove fixed max-width on parent, animate children instead or allow auto growth */
    transition: all 0.3s ease;
    box-shadow: none;
    text-decoration: none;
  }

  /* Add padding when expanded to look nice */
  .btn-header-pill:hover {
    padding-right: 16px;
    padding-left: 12px;
  }

  /* Specific fix for anchor tags behaving as flex containers */
  a.btn-header-pill {
    box-sizing: border-box;
  }

  .btn-header-pill .icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px; /* Fixed width for icon container */
    height: 32px;
    flex-shrink: 0;
  }

  .btn-header-pill .icon-wrapper svg {
    width: 16px;
    height: 16px;
  }

  /* Inherit text color for icons */
  .btn-header-pill .icon-wrapper svg path,
  .btn-header-pill .icon-wrapper svg circle {
    stroke: currentColor !important;
  }

  .btn-header-pill .text {
    max-width: 0;
    opacity: 0;
    margin-left: 0;
    overflow: hidden;
    white-space: nowrap;
    transition: all 0.3s ease;
    font-size: 13px;
    font-family: 'Poppins', sans-serif; /* Added Poppins font */
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .btn-header-pill:hover .text {
    max-width: 100px; /* Arbitrary large number for text to fit */
    opacity: 1;
    margin-left: 4px;
  }

  @media screen and (max-width: 1023px) {
    .btn-header-pill:hover .text {
      max-width: 0;
      opacity: 0;
      margin-left: 0;
    }
  }

  /* Center: Logo */
  .header-center {
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1;
  }

  .header-logo-link {
    display: block;
    line-height: 1;
    color: inherit;
    text-decoration: none;
  }

  .header-logo-image {
    display: block;
    height: auto;
    object-fit: contain;
    width: {{ logo_width }}px;
    max-height: calc(var(--header-height) - 20px); /* Ensure logo shrinks if needed */
    filter: brightness(0) invert(1);
    transition: filter 0.3s ease, width 0.3s ease, max-height 0.3s ease;
  }

  .header-custom.header-wrapper:not(.is-transparent) .header-logo-image {
    filter: brightness(0);
  }

  .header-custom.header-wrapper.is-scrolled .header-logo-image {
    filter: brightness(0) !important;
  }

  @media screen and (max-width: 767px) {
    .header-logo-image {
      width: {{ logo_width_mobile }}px;
    }
  }

  .header-logo-text {
    text-transform: uppercase;
    font-size: 24px;
    font-weight: bold;
    letter-spacing: 0.05em;
    color: var(--color-white);
  }

  /* Right: Actions */
  .header-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 12px;
    z-index: 2;
  }

  /* Cart Bubble Position Fix for Pill Button */
  .action-cart {
    position: relative;
  }

  /* Cart Bubble */
  .cart-count-bubble {
    position: absolute;
    background-color: var(--color-button);
    color: var(--color-button-text);
    height: 1.5rem;
    width: 1.5rem;
    border-radius: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 0.8rem;
    top: -5px;
    right: -10px;
    line-height: 1;
  }

  .action-cart {
    position: relative;
  }

  /* Mobile */
  @media screen and (max-width: 1023px) {
    .action-link span.visually-hidden-mobile {
      display: none;
    }
    .header-right {
      gap: 15px;
    }
    .action-account {
      display: none;
    }
    .action-favorite {
      display: none;
    }
  }

  /* Desktop Nav */
  .header-desktop-nav {
    display: none;
  }

  @media screen and (min-width: 1024px) {
    /* Hide hamburger on desktop */
    .header-left .btn-header-pill {
      display: none;
    }

    .header-desktop-nav {
      display: flex;
      align-items: center;
    }

    .header-desktop-nav > ul {
      display: flex;
      align-items: center;
      gap: 2px;
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .header-desktop-nav > ul > li {
      position: relative;
    }

    .header-desktop-nav > ul > li > a {
      position: relative;
      display: flex;
      align-items: center;
      height: var(--nav-btn-height);
      padding: 0 9px;
      font-size: 12px;
      font-family: 'Poppins', sans-serif;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: inherit;
      text-decoration: none;
      white-space: nowrap;
      transition: opacity 0.25s ease;
    }

    /* Underline reveal — grows from center */
    .header-desktop-nav > ul > li > a::before {
      content: '';
      position: absolute;
      left: 50%;
      bottom: calc(var(--nav-btn-height) / 2 - 0.7em);
      width: 0;
      height: 1px;
      background-color: currentColor;
      opacity: 0.85;
      transform: translateX(-50%);
      transition: width 0.25s ease;
      pointer-events: none;
    }

    .header-desktop-nav > ul > li > a:hover::before,
    .header-desktop-nav > ul > li.is-active > a::before {
      width: calc(100% - 18px);
    }

    .header-desktop-nav > ul > li.has-dropdown > a::after {
      content: '';
      display: inline-block;
      width: 5px;
      height: 5px;
      border-right: 1.5px solid currentColor;
      border-bottom: 1.5px solid currentColor;
      transform: rotate(45deg) translateY(-2px);
      margin-left: 5px;
      opacity: 0.5;
      vertical-align: middle;
      flex-shrink: 0;
    }

    /* Dropdown */
    .header-desktop-nav .nav-dropdown {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      padding-top: 4px;
      min-width: 160px;
      background-color: #fff;
      border: 1px solid rgba(0, 0, 0, 0.08);
      border-radius: 8px;
      padding: 6px 0;
      list-style: none;
      margin: 0;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
      z-index: 200;
    }

    .header-desktop-nav .has-dropdown:hover .nav-dropdown {
      display: block;
    }

    .header-desktop-nav .nav-dropdown li a {
      display: block;
      padding: 8px 16px;
      font-size: 13px;
      font-family: 'Poppins', sans-serif;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: #000;
      text-decoration: none;
      white-space: nowrap;
      transition: background-color 0.15s ease;
    }

    .header-desktop-nav .nav-dropdown li a:hover {
      background-color: rgba(0, 0, 0, 0.04);
    }
  }

  /* WhatsApp floating button — move to right side */
  /* TODO: inspect live site for exact selector, then apply:
     [app-selector] { left: auto !important; right: 20px !important; }
  */

  @media screen and (max-width: 1023px) {
    .header-currency {
      display: none;
    }
  }

  /* Custom currency UI */
  .header-currency {
    position: relative;
  }

  .action-currency {
    gap: 4px;
  }

  .action-currency .currency-code {
    font-size: 13px;
    font-family: 'Poppins', sans-serif;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    white-space: nowrap;
  }

  .action-currency .currency-caret {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 0;
    height: 0;
    border-left: 4px solid transparent;
    border-right: 4px solid transparent;
    border-top: 4px solid currentColor;
    flex-shrink: 0;
    margin-left: 2px;
  }

  .currency-dropdown {
    display: none;
    position: absolute;
    top: calc(100% + 4px);
    left: 50%;
    transform: translateX(-50%);
    min-width: 200px;
    background-color: #fff;
    border: 1px solid rgba(0, 0, 0, 0.08);
    border-radius: 8px;
    padding: 6px 0;
    list-style: none;
    margin: 0;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    z-index: 300;
    color: #333;
  }

  .currency-dropdown.open {
    display: block;
  }

  .currency-dropdown li {
    padding: 8px 16px;
    font-size: 13px;
    font-family: 'Poppins', sans-serif;
    font-weight: 500;
    cursor: pointer;
    white-space: nowrap;
    transition: background-color 0.15s ease;
  }

  .currency-dropdown li:hover {
    background-color: rgba(0, 0, 0, 0.04);
  }

  .currency-dropdown li.is-active {
    font-weight: 700;
  }

  /* =========================================================
     AVELLE ADVANCED CART DRAWER
     Fixed: header + checkout only
     Scrolls naturally: rewards, products, offers, discounts,
     totals and trust messages
     ========================================================= */

  .cart-drawer__dialog:not(.cart-drawer--empty) .cart-drawer__inner,
  .cart-drawer__dialog:not(.cart-drawer--empty) .cart-items-component {
    height: 100%;
    min-height: 0;
  }

  .cart-drawer__dialog:not(.cart-drawer--empty) .cart-items-component {
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-fixed-header {
    flex: 0 0 auto;
    position: relative !important;
    top: auto !important;
    z-index: 4;
    background: rgb(var(--color-background-rgb));
  }

  .avelle-cart-header-count {
    font: inherit;
  }

  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-layout {
    display: grid;
    grid-template-rows: minmax(0, 1fr) auto;
    flex: 1 1 auto;
    min-height: 0;
    overflow: hidden;
  }

  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll {
    display: block !important;
    min-height: 0 !important;
    height: auto !important;
    max-height: none !important;
    overflow-x: hidden !important;
    overflow-y: auto !important;
    overscroll-behavior: contain;
    -webkit-overflow-scrolling: touch;
    scrollbar-gutter: stable;
    padding-bottom: 18px;
  }

  /* Nothing inside the scrolling area is sticky */
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll .cart-drawer__summary,
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll .cart__summary-totals,
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll .cart__ctas,
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll .avelle-cart-extra,
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll .avelle-cart-trust {
    position: static !important;
    inset: auto !important;
    transform: none !important;
  }

  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-summary-scroll {
    height: auto !important;
    min-height: 0 !important;
    max-height: none !important;
    overflow: visible !important;
  }

  /* Hide the original checkout button only in the drawer.
     The cart page keeps its normal checkout button. */
  .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-summary-scroll .cart__ctas {
    display: none !important;
  }

  .avelle-cart-fixed-checkout {
    position: relative;
    z-index: 6;
    flex: 0 0 auto;
    background: rgb(var(--color-background-rgb));
    border-top: 1px solid rgb(var(--color-border-rgb) / 0.7);
    padding: 12px 16px calc(12px + env(safe-area-inset-bottom));
    box-shadow: 0 -8px 24px rgb(0 0 0 / 0.05);
  }

  .avelle-cart-fixed-checkout__button {
    width: 100%;
    min-height: 52px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding-inline: 20px;
    background: #000;
    color: #fff;
    border: 1px solid #000;
    border-radius: 0;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.06em;
  }

  .avelle-cart-bottom-spacer {
    height: 4px;
  }

  /* Clean quantity: − [1] +   trash */
  .cart-drawer .cart-items__quantity {
    width: auto !important;
    min-width: 0 !important;
  }

  .cart-drawer .cart-items__quantity-controls {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: flex-end !important;
    gap: 10px !important;
    width: auto !important;
    min-width: 0 !important;
  }

  .cart-drawer .cart-items__quantity .quantity-selector {
    display: inline-grid !important;
    grid-template-columns:
      {{ section.settings.cart_quantity_size_desktop }}px
      {{ section.settings.cart_quantity_size_desktop }}px
      {{ section.settings.cart_quantity_size_desktop }}px;
    align-items: center;
    width: auto !important;
    min-width: 0 !important;
    max-width: none !important;
    height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    min-height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    border: 0 !important;
    border-radius: 0 !important;
    background: transparent !important;
    overflow: visible !important;
    box-shadow: none !important;
  }

  .cart-drawer .cart-items__quantity .quantity-selector button,
  .cart-drawer .cart-items__quantity .quantity-selector input {
    width: {{ section.settings.cart_quantity_size_desktop }}px !important;
    min-width: {{ section.settings.cart_quantity_size_desktop }}px !important;
    max-width: {{ section.settings.cart_quantity_size_desktop }}px !important;
    height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    min-height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    padding: 0 !important;
    margin: 0 !important;
    border: 0 !important;
    border-radius: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
    text-align: center !important;
  }

  .cart-drawer .cart-items__quantity .quantity-selector input {
    border: 1px solid rgb(var(--color-foreground-rgb) / 0.28) !important;
    border-radius: 4px !important;
    -moz-appearance: textfield;
  }

  .cart-drawer .cart-items__quantity .quantity-selector input::-webkit-outer-spin-button,
  .cart-drawer .cart-items__quantity .quantity-selector input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  .cart-drawer .cart-items__remove {
    flex: 0 0 {{ section.settings.cart_quantity_size_desktop }}px !important;
    width: {{ section.settings.cart_quantity_size_desktop }}px !important;
    min-width: {{ section.settings.cart_quantity_size_desktop }}px !important;
    height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    min-height: {{ section.settings.cart_quantity_size_desktop }}px !important;
    padding: 0 !important;
    margin: 0 !important;
  }

  /* Product thumbnails: consistent box, never zoom/crop */
  .cart-drawer .cart-items__media {
    width: {{ section.settings.cart_product_image_width_desktop }}px !important;
  }

  .cart-drawer .cart-items__media-container {
    width: {{ section.settings.cart_product_image_width_desktop }}px !important;
    height: calc({{ section.settings.cart_product_image_width_desktop }}px * 1.3333) !important;
    aspect-ratio: auto !important;
    display: flex !important;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .cart-drawer .cart-items__media-image {
    width: 100% !important;
    height: 100% !important;
    object-fit: contain !important;
    object-position: center !important;
    aspect-ratio: auto !important;
  }

  @media screen and (max-width: 749px) {
    .cart-drawer__dialog:not(.cart-drawer--empty) .avelle-cart-scroll {
      scrollbar-gutter: auto;
      padding-bottom: 14px;
    }

    .avelle-cart-fixed-checkout {
      padding: 10px 12px calc(10px + env(safe-area-inset-bottom));
    }

    .avelle-cart-fixed-checkout__button {
      min-height: 50px;
      padding-inline: 16px;
      font-size: 11px;
    }

    .cart-drawer .cart-items__quantity .quantity-selector {
      grid-template-columns:
        {{ section.settings.cart_quantity_size_mobile }}px
        {{ section.settings.cart_quantity_size_mobile }}px
        {{ section.settings.cart_quantity_size_mobile }}px;
      height: {{ section.settings.cart_quantity_size_mobile }}px !important;
      min-height: {{ section.settings.cart_quantity_size_mobile }}px !important;
    }

    .cart-drawer .cart-items__quantity .quantity-selector button,
    .cart-drawer .cart-items__quantity .quantity-selector input {
      width: {{ section.settings.cart_quantity_size_mobile }}px !important;
      min-width: {{ section.settings.cart_quantity_size_mobile }}px !important;
      max-width: {{ section.settings.cart_quantity_size_mobile }}px !important;
      height: {{ section.settings.cart_quantity_size_mobile }}px !important;
      min-height: {{ section.settings.cart_quantity_size_mobile }}px !important;
    }

    .cart-drawer .cart-items__remove {
      flex-basis: {{ section.settings.cart_quantity_size_mobile }}px !important;
      width: {{ section.settings.cart_quantity_size_mobile }}px !important;
      min-width: {{ section.settings.cart_quantity_size_mobile }}px !important;
      height: {{ section.settings.cart_quantity_size_mobile }}px !important;
      min-height: {{ section.settings.cart_quantity_size_mobile }}px !important;
    }

    .cart-drawer .cart-items__media {
      width: {{ section.settings.cart_product_image_width_mobile }}px !important;
    }

    .cart-drawer .cart-items__media-container {
      width: {{ section.settings.cart_product_image_width_mobile }}px !important;
      height: calc({{ section.settings.cart_product_image_width_mobile }}px * 1.3333) !important;
    }
  }

{% endstyle %}

{%- if section.settings.enable_custom_currency_ui -%}
  <style>
    .bacurr-cur-blocker { display: none !important; }
  </style>
{%- endif -%}

<header
  class="header-custom header-wrapper {% if transparent_header %}is-transparent{% endif %}"
  data-sticky-type="{{ sticky_header_type }}"
  data-template-name="{{ template.name }}"
  id="site-header"
>
  <div class="header-inner">
    <!-- LEFT -->
    <div class="header-left">
      {%- if section.settings.menu != blank -%}
        {% render 'header-drawer', linklist: section.settings.menu %}

        <button
          class="btn-header-pill group"
          onclick="const s = this.parentNode.querySelector('summary'); if(s) s.click()"
          aria-label="Menu"
        >
          <span class="icon-wrapper">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M1 3.5H13" stroke-width="1.5" stroke-linecap="round"/>
              <path d="M1 10.5H13" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </span>
          <span class="text">Menu</span>
        </button>

        <nav class="header-desktop-nav" aria-label="Main navigation">
          <ul>
            {%- for link in section.settings.menu.links -%}
              <li class="{% if link.links != blank %}has-dropdown{% endif %}{% if link.active or link.current %} is-active{% endif %}">
                <a href="{{ link.url }}">{{ link.title | escape }}</a>
                {%- if link.links != blank -%}
                  <ul class="nav-dropdown">
                    {%- for child_link in link.links -%}
                      <li><a href="{{ child_link.url }}">{{ child_link.title | escape }}</a></li>
                    {%- endfor -%}
                  </ul>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </ul>
        </nav>
      {%- endif -%}
    </div>

    <!-- CENTER -->
    <div class="header-center">
      <a href="{{ routes.root_url }}" class="header-logo-link">
        {%- if logo != blank -%}
          {%- assign image_size_2x = logo_width | times: 2 | at_most: 5760 -%}
          <img
            srcset="{{ logo | image_url: width: logo_width }} 1x, {{ logo | image_url: width: image_size_2x }} 2x"
            src="{{ logo | image_url: width: logo_width }}"
            loading="eager"
            width="{{ logo_width }}"
            height="{{ logo_height }}"
            alt="{{ logo.alt | default: shop.name | escape }}"
            class="header-logo-image"
          >
        {%- else -%}
          <span class="h2 header-logo-text">{{ shop.name }}</span>
        {%- endif -%}
      </a>
    </div>

    <!-- RIGHT -->
    <div class="header-right">
      {%- if section.settings.enable_custom_currency_ui -%}
        <div class="header-currency" id="header-currency-switcher">
          <button class="btn-header-pill action-currency" aria-label="Currency" aria-haspopup="listbox" aria-expanded="false">
            <span class="currency-code">IDR</span>
            <span class="currency-caret" aria-hidden="true"></span>
          </button>
          <ul class="currency-dropdown" role="listbox" aria-label="Select currency">
            <!-- Populated by JS from Nova's #baCurrSelector -->
          </ul>
        </div>
      {%- endif -%}

      {%- if shop.customer_accounts_enabled -%}
        <a
          href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
          class="btn-header-pill action-account"
        >
          <span class="icon-wrapper">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M7 7C8.65685 7 10 5.65685 10 4C10 2.34315 8.65685 1 7 1C5.34315 1 4 2.34315 4 4C4 5.65685 5.34315 7 7 7Z" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M13 13C13 10.2386 10.3137 8 7 8C3.68629 8 1 10.2386 1 13" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
          <span class="text">Sign In</span>
        </a>
      {%- endif -%}

      <button
        class="btn-header-pill action-search"
        onclick="const sm = document.getElementById('search-modal'); if(sm) sm.showDialog()"
        aria-label="{{ 'content.search' | t }}"
      >
        <span class="icon-wrapper">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
            <circle cx="6" cy="6" r="4.5" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M12.5 12.5L9.5 9.5" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
        <span class="text">Search</span>
      </button>

      <a href="/a/wishlist" class="btn-header-pill action-favorite" aria-label="Favorites">
        <span class="icon-wrapper">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M7 12C7 12 1 8.5 1 4.5C1 2.567 2.567 1 4.5 1C5.59 1 6.56 1.48 7 2.25C7.44 1.48 8.41 1 9.5 1C11.433 1 13 2.567 13 4.5C13 8.5 7 12 7 12Z" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
        <span class="text">Favorites</span>
      </a>

      {%- if settings.cart_type == 'drawer' and template.name != 'cart' -%}
        <script src="{{ 'cart-drawer.js' | asset_url }}" type="module" fetchpriority="low"></script>

        {% capture empty_cart_drawer_content %}
          <div class="cart-drawer__header">
            <button
              ref="closeButton"
              on:click="cart-drawer-component/close"
              class="button close-button cart-drawer__close-button button-unstyled"
              aria-label="{{ 'actions.close' | t }}"
            >
              <span class="svg-wrapper">
                {{- 'icon-close.svg' | inline_asset_content -}}
              </span>
            </button>
          </div>

          <div
            class="cart-drawer__content"
            aria-label="{{ 'content.cart_title' | t }}"
          >
            <h2 class="cart-drawer__heading h4 cart-drawer__heading--empty">
              {{ 'content.your_cart_is_empty' | t }}
            </h2>

            <div class="cart-drawer__items">
              {% render 'cart-products', force_empty: true %}
            </div>
          </div>
        {% endcapture %}

        <cart-drawer-component
          class="cart-drawer"
          {% if settings.auto_open_cart_drawer %}
            auto-open
          {% endif %}
        >
          <template id="empty-cart-template">
            {{ empty_cart_drawer_content }}
          </template>

          <button
            class="btn-header-pill action-cart"
            id="cart-icon-bubble"
            on:click="/open"
            aria-haspopup="dialog"
            aria-label="{{ 'content.cart_title' | t }}"
          >
            <span class="icon-wrapper">
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1H2.5L3.9 8.02C3.96 8.32 4.09 8.59 4.29 8.81C4.49 9.03 4.75 9.17 5.05 9.22H11.55C11.85 9.17 12.11 9.03 12.31 8.81C12.51 8.59 12.64 8.32 12.7 8.02L13.5 4H3" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                <circle cx="5" cy="12" r="1" stroke-width="1.5"/>
                <circle cx="11" cy="12" r="1" stroke-width="1.5"/>
              </svg>
            </span>
            <span class="text">{{ cart.item_count }} Items</span>
            {%- if cart != empty and cart.item_count > 0 -%}
              <div class="cart-count-bubble">
                <span aria-hidden="true">{{ cart.item_count }}</span>
              </div>
            {%- endif -%}
          </button>

          <dialog
            ref="dialog"
            class="cart-drawer__dialog dialog-modal dialog-drawer color-{{ settings.drawer_color_scheme }}{% if cart.empty? %} cart-drawer--empty{%endif%}"
            aria-labelledby="cart-drawer-heading"
            scroll-lock
            cart-summary-sticky="false"
          >
            <div class="cart-drawer__inner" data-hydration-key="cart-drawer-inner">
              <cart-items-component class="cart-items-component" data-drawer data-section-id="{{ section.id }}">
                {%- if cart.empty? -%}
                  {{ empty_cart_drawer_content }}
                {%- else -%}
                  <div class="cart-drawer__header avelle-cart-fixed-header" id="cart-drawer-header">
                    <h2 class="cart-drawer__heading h4" id="cart-drawer-heading">
                      {{ 'content.cart_title' | t }} <span class="avelle-cart-header-count">{{ cart.item_count }}</span>
                    </h2>
                    <button
                      ref="closeButton"
                      on:click="cart-drawer-component/close"
                      class="button close-button cart-drawer__close-button button-unstyled"
                      aria-label="{{ 'actions.close' | t }}"
                    >
                      <span class="svg-wrapper">
                        {{- 'icon-close.svg' | inline_asset_content -}}
                      </span>
                    </button>
                  </div>

                  <div class="avelle-cart-layout">
                    <scroll-hint
                      class="cart-drawer__content avelle-cart-scroll"
                      aria-label="{{ 'content.cart_title' | t }}"
                      style="--header-height: 60px;"
                    >
                      {% render 'advanced-cart-extras',
                        position: 'top',
                        offer_enabled: section.settings.cart_offer_enabled,
                        offer_text: section.settings.cart_offer_text,
                        reward_1_amount: section.settings.cart_reward_1_amount,
                        reward_1_text: section.settings.cart_reward_1_text,
                        reward_2_amount: section.settings.cart_reward_2_amount,
                        reward_2_text: section.settings.cart_reward_2_text,
                        reward_3_amount: section.settings.cart_reward_3_amount,
                        reward_3_text: section.settings.cart_reward_3_text,
                        quantity_size_desktop: section.settings.cart_quantity_size_desktop,
                        quantity_size_mobile: section.settings.cart_quantity_size_mobile,
                        product_image_width_desktop: section.settings.cart_product_image_width_desktop,
                        product_image_width_mobile: section.settings.cart_product_image_width_mobile
                      %}

                      <div class="cart-drawer__items">
                        {% render 'cart-products' %}
                      </div>

                      {% render 'advanced-cart-extras',
                        position: 'offers',
                        show_available_codes: section.settings.cart_show_available_codes,
                        discount_heading: section.settings.cart_discount_heading,
                        discount_code_1: section.settings.cart_discount_code_1,
                        discount_code_1_text: section.settings.cart_discount_code_1_text,
                        discount_code_2: section.settings.cart_discount_code_2,
                        discount_code_2_text: section.settings.cart_discount_code_2_text,
                        discount_code_3: section.settings.cart_discount_code_3,
                        discount_code_3_text: section.settings.cart_discount_code_3_text
                      %}

                      <div class="cart-drawer__summary avelle-cart-summary-scroll">
                        {% render 'cart-summary', section_id: section.id %}
                      </div>

                      {% render 'advanced-cart-extras',
                        position: 'bottom',
                        trust_1_title: section.settings.cart_trust_1_title,
                        trust_1_text: section.settings.cart_trust_1_text,
                        trust_2_title: section.settings.cart_trust_2_title,
                        trust_2_text: section.settings.cart_trust_2_text,
                        trust_3_title: section.settings.cart_trust_3_title,
                        trust_3_text: section.settings.cart_trust_3_text
                      %}

                      <div class="avelle-cart-bottom-spacer" aria-hidden="true"></div>
                    </scroll-hint>

                    <div class="avelle-cart-fixed-checkout">
                      <button
                        type="submit"
                        form="cart-form"
                        name="checkout"
                        class="button avelle-cart-fixed-checkout__button"
                      >
                        <span>CHECK OUT</span>
                        <span>{{ cart.total_price | money }}</span>
                      </button>
                    </div>
                  </div>
                {%- endif -%}
              </cart-items-component>
            </div>
          </dialog>
        </cart-drawer-component>
      {%- else -%}
        <a href="{{ routes.cart_url }}" class="btn-header-pill action-cart" id="cart-icon-bubble">
          <span class="icon-wrapper">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M1 1H2.5L3.9 8.02C3.96 8.32 4.09 8.59 4.29 8.81C4.49 9.03 4.75 9.17 5.05 9.22H11.55C11.85 9.17 12.11 9.03 12.31 8.81C12.51 8.59 12.64 8.32 12.7 8.02L13.5 4H3" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <circle cx="5" cy="12" r="1" stroke-width="1.5"/>
              <circle cx="11" cy="12" r="1" stroke-width="1.5"/>
            </svg>
          </span>
          <span class="text">{{ cart.item_count }} Items</span>
          {%- if cart != empty and cart.item_count > 0 -%}
            <div class="cart-count-bubble">
              <span aria-hidden="true">{{ cart.item_count }}</span>
            </div>
          {%- endif -%}
        </a>
      {%- endif -%}
    </div>
  </div>
</header>
<script>
  (() => {
    const header = document.getElementById('site-header');
    if (!header) return;

    // Dynamically calculate the offset needed for transparent heading
    const setHeaderOffset = () => {
        const headerGroup = document.getElementById('header-group');
        if (headerGroup) {
            let offsetHeight = 0;
            const headerSection = header.closest('.shopify-section') || header;
            for (let child of headerGroup.children) {
                if (child === headerSection) break;
                offsetHeight += child.offsetHeight;
            }
            document.documentElement.style.setProperty('--announcement-bar-height', `${offsetHeight}px`);
            // Total height of the permanently-sticky header stack (announcement + header).
            // Consumed by sticky page content (e.g. product details column) so it
            // parks just below the pinned header instead of hiding behind it.
            document.documentElement.style.setProperty('--header-group-height', `${headerGroup.offsetHeight}px`);
        }
    };
    setHeaderOffset();
    window.addEventListener('resize', setHeaderOffset);

    // Sticky header logic
    const stickyType = header.dataset.stickyType;
    let lastScrollY = window.scrollY;
    let ticking = false;

    // Check initial transparency state
    const isInitiallyTransparent = {{ transparent_header | json }};

    const updateHeader = () => {
        const currentScrollY = window.scrollY;

        // Add solid background if scrolled more than 50px
        if (currentScrollY > 50) {
            header.classList.add('is-scrolled');
        } else {
            header.classList.remove('is-scrolled');
        }

        const isScrollingDown = currentScrollY > lastScrollY;
        const scrollDifference = Math.abs(currentScrollY - lastScrollY);

        // Sticky Logic - Hide/Show on scroll via class instead of inline transform
        // This is much safer and respects CSS specificity like !important rules
        if (stickyType === 'on-scroll-up') {
             if (isScrollingDown && currentScrollY > 150 && scrollDifference > 5) {
                 header.classList.add('is-hidden');
             } else if (!isScrollingDown && scrollDifference > 5) {
                 header.classList.remove('is-hidden');
             }
        } else if (stickyType === 'always') {
             header.classList.remove('is-hidden');
        }

        lastScrollY = currentScrollY;
        ticking = false;
    };

    // Run once on load to ensure initial state is correct
    updateHeader();

    const handleScroll = () => {
        if (!ticking) {
            window.requestAnimationFrame(updateHeader);
            ticking = true;
        }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    // Run once on load
    updateHeader();
  })();
</script>

{%- if section.settings.enable_custom_currency_ui -%}
<script>
  (() => {
    function initCurrencySwitcher(switcher, { floating }) {
      if (!switcher) return null;

      const btn = switcher.querySelector(floating ? '.action-currency' : '.drawer-currency-toggle');
      const label = switcher.querySelector(floating ? '.currency-code' : '.drawer-currency-label');
      const dropdown = switcher.querySelector(floating ? '.currency-dropdown' : '.drawer-currency-list');
      const caret = floating ? null : switcher.querySelector('.icon-caret--forward');

      function syncAll(value) {
        switcher.querySelectorAll('li').forEach(el => el.classList.toggle('is-active', el.dataset.currency === value));
        if (label) label.textContent = value;
      }

      function triggerNova(value, select) {
        const novaItem = document.querySelector(`.bacurr-dropdown-choices .currMovers[id="${value}"]`);
        if (novaItem) {
          novaItem.click();
        } else {
          select.value = value;
          select.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      function buildList(select) {
        dropdown.innerHTML = '';
        if (floating) dropdown.removeAttribute('hidden');
        Array.from(select.options).forEach(opt => {
          const li = document.createElement('li');
          li.textContent = opt.text;
          li.dataset.currency = opt.value;
          li.setAttribute('role', 'option');
          if (opt.selected) {
            if (label) label.textContent = opt.value;
            li.classList.add('is-active');
          }
          li.addEventListener('click', () => {
            triggerNova(opt.value, select);
            syncAll(opt.value);
            if (floating) closeDropdown();
          });
          dropdown.appendChild(li);
        });
      }

      function openDropdown() {
        if (floating) {
          dropdown.classList.add('open');
        } else {
          dropdown.removeAttribute('hidden');
          if (caret) caret.style.transform = 'rotate(90deg)';
        }
        btn.setAttribute('aria-expanded', 'true');
      }

      function closeDropdown() {
        if (floating) {
          dropdown.classList.remove('open');
        } else {
          dropdown.setAttribute('hidden', '');
          if (caret) caret.style.transform = '';
        }
        btn.setAttribute('aria-expanded', 'false');
      }

      btn.addEventListener('click', e => {
        e.stopPropagation();
        btn.getAttribute('aria-expanded') === 'true' ? closeDropdown() : openDropdown();
      });

      if (floating) {
        document.addEventListener('click', closeDropdown);
        document.addEventListener('keydown', e => { if (e.key === 'Escape') closeDropdown(); });
      }

      return { buildList, syncAll };
    }

    const headerSwitcher = initCurrencySwitcher(
      document.getElementById('header-currency-switcher'), { floating: true }
    );
    const drawerSwitcher = initCurrencySwitcher(
      document.getElementById('drawer-currency-switcher'), { floating: false }
    );

    function waitForNova(attempts) {
      const select = document.getElementById('baCurrSelector');
      if (select) {
        if (headerSwitcher) headerSwitcher.buildList(select);
        if (drawerSwitcher) drawerSwitcher.buildList(select);
        select.addEventListener('change', () => {
          if (headerSwitcher) headerSwitcher.syncAll(select.value);
          if (drawerSwitcher) drawerSwitcher.syncAll(select.value);
        });
        return;
      }
      if (attempts > 0) setTimeout(() => waitForNova(attempts - 1), 400);
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', () => waitForNova(20));
    } else {
      waitForNova(20);
    }
  })();
</script>
{%- endif -%}
